#ifndef CONSTANTSH
#define CONSTANTSH

namespace boliao {
    const auto BALL_VEL = 210.f;
    const auto PADDLE_VEL = 320.f;
    const auto BALL_RADIUS = 10.f;
    const auto WINDOW_WIDTH = 1024;
    const auto WINDOW_HEIGHT = 640;
    const auto PADDLE_WIDTH = 200.f;
    const auto PADDLE_HEIGHT = 20.f;
    const auto BRICK_WIDTH = 50.f;
    const auto BRICK_HEIGHT = 20.f;
    const auto NUMBRICKS_X = 15;
    const auto NUMBRICKS_Y = 5;
    const auto BRICK_SPACING = 5;
    const auto BRICK_OFFSET_X = 70;
}

#endif

